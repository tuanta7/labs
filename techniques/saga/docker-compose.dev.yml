services:
  postgres:
    image: postgres:18.0-alpine
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    restart: unless-stopped

  mongodb:
    image: mongo:8.0-noble
    container_name: saga-mongodb
    ports:
      - "27017:27017"
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:4.1.2-management
    container_name: saga-rabbitmq-ui
    ports:
      - "5672:5672"
      - "15672:15672" # rabbitmq management
    environment:
      - RABBITMQ_DEFAULT_USER=rabbitmq
      - RABBITMQ_DEFAULT_PASS=password
      - RABBITMQ_DEFAULT_VHOST=/
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "rabbitmq-diagnostics", "ping" ]
      interval: 30s
      timeout: 10s
      retries: 5

#  orchestrator:
#    build:
#      context: ./orchestrator
#      dockerfile: Dockerfile
#    container_name: orchestrator
#    ports:
#      - "8080:8080"
#    environment:
#      RABBITMQ_URL: amqp://rabbitmq:password@rabbitmq:5672/
#      MONGO_URI: mongodb://mongodb:27017
#      HTTP_ADDR: ":8080"
#    depends_on:
#      rabbitmq:
#        condition: service_healthy
#      mongodb:
#        condition: service_started
#    restart: unless-stopped
#
#  reserve-service:
#    build:
#      context: ./services/reserve
#      dockerfile: Dockerfile
#    container_name: reserve-service
#    environment:
#      RABBITMQ_URL: amqp://rabbitmq:password@rabbitmq:5672/
#    depends_on:
#      rabbitmq:
#        condition: service_healthy
#    restart: unless-stopped
#
#  payment-service:
#    build:
#      context: ./services/payment
#      dockerfile: Dockerfile
#    container_name: payment-service
#    environment:
#      RABBITMQ_URL: amqp://rabbitmq:password@rabbitmq:5672/
#    depends_on:
#      rabbitmq:
#        condition: service_healthy
#    restart: unless-stopped
#
#  charging-service:
#    build:
#      context: ./services/charging
#      dockerfile: Dockerfile
#    container_name: charging-service
#    environment:
#      RABBITMQ_URL: amqp://rabbitmq:password@rabbitmq:5672/
#    depends_on:
#      rabbitmq:
#        condition: service_healthy
#    restart: unless-stopped
